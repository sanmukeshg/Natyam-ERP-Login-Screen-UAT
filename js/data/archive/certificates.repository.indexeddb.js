/**
 * ARCHIVED — superseded by js/data/certificates.repository.firestore.js as of
 * Milestone 18 (Certificates → Cloud Firestore, see ADR-014).
 *
 * Not imported by any active code. Kept, not deleted, for rollback: if the
 * Firestore migration needs to be reverted, this is the exact IndexedDB
 * implementation `certificates$` pointed to immediately before that happened.
 * It depends on `Repository` (js/core/repository.js) and the `certificates`
 * IndexedDB store declared in `SCHEMA` (js/config/app.config.js) — both
 * still present and untouched, so this file would work again unmodified if
 * re-wired back into js/data/repositories.js's `certificates$` export.
 *
 * Do not edit this file to fix bugs or add features — it is a snapshot,
 * not a maintained implementation. Fix the active Firestore repository
 * instead.
 */

import { Repository } from '../../core/repository.js';

export class CertificateRepository extends Repository {
    constructor() {
        super({ store: 'certificates', prefix: 'CRT', entity: 'Certificate', searchFields: ['serial', 'studentName', 'title'] });
    }

    validate(record) {
        if (!record.studentId) throw new Error('A certificate must name a student.');
        if (!record.title?.trim()) throw new Error('A certificate needs a title.');
        if (!record.serial) throw new Error('A certificate needs a serial number.');
    }

    async forStudent(studentId) {
        return (await this.where('studentId', studentId)).sort((a, b) => b.issuedOn.localeCompare(a.issuedOn));
    }

    /** Public verification: serial in, certificate or null out. */
    async verify(serial) {
        const rows = await this.where('serial', String(serial || '').trim().toUpperCase());
        return rows[0] || null;
    }
}
